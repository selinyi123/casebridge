# v0.1.16.4 Repository Test Migration Checklist

## Completed

- Assessment version repository test migrated to `test_session`.
- Closure repository test migrated to `test_session`.
- Service plan repository test migrated to `test_session`.
- Supervisor review repository test migrated to `test_session`.
- Repository tests now seed dedicated case ids instead of using `CASE-0001`.
- Existing audit writes and real repository functions are preserved.

## Acceptance

- Core repository tests no longer open default `SessionLocal` directly.
- Core repository tests use the temporary SQLite test database fixture.
- Role and audit behavior are not mocked away.
- Test data uses dedicated case ids.

## Limitations

- GitHub code search returned no remaining `SessionLocal` matches, but repository indexing is not fully reliable.
- A local grep or CI run is still needed for complete confirmation.
- Transaction rollback per test is not implemented yet.
