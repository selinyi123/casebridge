# v0.1.16.6 Test Teardown Assertions Checklist

## Completed

- Added transaction teardown assertion to `test_session`.
- Added connection closed assertion to `test_session`.
- Changed `isolated_client` cleanup to remove only the `get_db` override.
- Added assertion that `get_db` override does not leak after `isolated_client` teardown.

## Acceptance

- A leaked FastAPI `get_db` override now fails test teardown.
- An active external test transaction now fails test teardown.
- An unclosed test DB connection now fails test teardown.
- Existing route and repository tests still use real repositories and audit writes.

## Limitations

- No CI run was available to confirm execution.
- `docs/development/test-isolation.md` full rewrite was blocked by safety checks; this checklist records the implementation state.
- Optional residue checks against selected tables remain future work.
