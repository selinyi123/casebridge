# v0.1.16.5 Transaction Rollback Test Session Checklist

## Completed

- `test_session` now uses a connection-level transaction.
- Test Session is bound to that connection with `join_transaction_mode="create_savepoint"`.
- Code under test can still call `commit()`.
- Test teardown closes the Session, rolls back the external transaction, closes the connection, and disposes the engine.
- `isolated_client` continues to override FastAPI `get_db` and clear overrides after each test.
- Test isolation documentation updated.

## Acceptance

- Route and repository tests still use real repositories.
- Audit writes are preserved inside the test transaction.
- Default demo database is not touched by closure/reopen route tests.
- Outer transaction rollback is in place for test cleanup.

## Limitations

- No CI run was available to confirm execution.
- Teardown assertions for zero residue are not implemented yet.
- Broader repository test migration should continue if new tests are added.
