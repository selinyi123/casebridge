# v0.1.13.1 Plan Timeline and Create UI Checklist

## Completed

- Service evidence chain now includes chronological `events`.
- Service plan create UI exists at `/clients/c-0001/plans/create`.
- Intervention create UI exists at `/clients/c-0001/interventions/create`.
- Repository test asserts evidence-chain events include plan and intervention records.

## API surfaces

- `POST /api/v1/cases/CASE-0001/goals/plans`
- `POST /api/v1/cases/CASE-0001/goals/interventions`
- `GET /api/v1/cases/CASE-0001/goals/evidence-chain`

## Acceptance

- Plan and intervention writes stay manual and authenticated.
- Evidence chain remains manual-only.
- AI does not generate plan, intervention, outcome, or closure status.

## Limitations

- Direct unified timeline router update was blocked; evidence-chain `events` is the stable substitute for this iteration.
- UI still requires manual bearer token input.
- Route-level API tests are still not implemented.
