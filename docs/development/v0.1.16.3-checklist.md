# v0.1.16.3 Full Test DB Override Checklist

## Completed

- Temporary SQLite test database fixture.
- FastAPI `get_db` dependency override.
- Test schema initialization through SQLAlchemy metadata.
- Test-only demo users seeded into the isolated database.
- Closure route test migrated to `isolated_client`.
- Reopen route test migrated to `isolated_client`.
- Route data factory accepts injected test session.
- Test isolation documentation updated.

## Acceptance

- Closure state route test no longer uses the default app database.
- Reopen state route test no longer uses the default app database.
- `app.dependency_overrides` is cleared after each test.
- Role gates remain active.
- Audit writes remain active.

## Limitations

- Full transaction rollback per test is not implemented.
- Existing repository-level tests still use the default SessionLocal unless migrated later.
- CI status still needs to be verified by GitHub checks.
